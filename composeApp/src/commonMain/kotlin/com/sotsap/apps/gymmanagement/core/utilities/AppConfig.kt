package com.sotsap.apps.gymmanagement.core.utilities

/**
 * Configuration object for the application.
 *
 * This object holds various constants and settings that control the behavior
 * of the application. These settings might include things like API endpoints,
 * feature flags, or debugging options.
 */
object AppConfig {

    /**
     * A flag indicating whether the application is running in debug mode.
     * Set to `true` to enable debug-specific features, logging, or behavior.
     * Set to `false` for production builds.
     */
    const val DEBUG = true

}